My view is that the following items can simply be enum lists
Gender
DriverLicense
Race
BusinessType
WeekHour
JobType (full-time/part-time)

Year of study - make sure that they can always select the current year (ie when it gets to 2024, a student may select that as the current year).
